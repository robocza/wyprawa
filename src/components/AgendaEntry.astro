---
interface Props {
    "entry": {
        "time": string,
        "lecturer": string,
        "title": string,
        "description": string
    }
}

const { entry } = Astro.props;
---
<div class="py-8">
    <h3>{entry.title}</h3>
    <h3>{entry.lecturer}</h3>
    <span class="time">{entry.time}</span>
    <p class="mt-0">
        {entry.description}
    </p>
</div>
