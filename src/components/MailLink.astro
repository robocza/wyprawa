---
import Link from '@components/Link.astro';

interface Props {
    email: string;
    className?: string;
}

const { email, className } = Astro.props;
const url = 'mailto:' + email;
---

<Link url={url} text={email} className={className} />
